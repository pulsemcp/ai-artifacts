{
  "name": "trace-capture",
  "version": "1.0.0",
  "private": true,
  "description": "AIR hook for capturing agent session transcripts to cloud storage",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "rebuild": "npm run clean && npm run build"
  },
  "devDependencies": {
    "typescript": "~5.5.0",
    "@types/node": "~20.0.0"
  }
}
